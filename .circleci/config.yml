version: 2.1

jobs:
  build:
    docker:
      - image: cimg/node:18.20.2-browsers
    working_directory: ~/project/ui-tests  # FIXED: correct folder

    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Run Chrome tests
          command: npm run test-chrome
