version: 2.1
orbs:
  browser-tools: circleci/browser-tools@1.4.0

jobs:
  test:
    docker:
      - image: cimg/node:lts  # Base Node image
    steps:
      - checkout
      - browser-tools/install-browser-tools  # Installs both Chrome and ChromeDriver
      - run:
          name: Verify installations
          command: |
            echo "Chrome path: $(which google-chrome)"
            google-chrome --version
            echo "ChromeDriver path: $(which chromedriver)"
            chromedriver --version
      - run:
          name: Install dependencies
          command: npm ci
      - run:
          name: Run tests
          command: npm test
      - store_artifacts:
          path: tests_output
      - store_artifacts:
          path: screenshots

workflows:
  test-suite:
    jobs:
      - test