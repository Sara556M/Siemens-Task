{
  "name": "ui-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "./node_modules/.bin/nightwatch --env chrome_headless tests",
    "test:debug": "./node_modules/.bin/nightwatch --env chrome tests"
  },
  "devDependencies": {
    "nightwatch": "^3.12.2",
    "chromedriver": "^120.0.0"
  }
}