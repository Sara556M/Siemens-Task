{
  "name": "ui-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "nightwatch --env chrome_headless tests",
    "test:debug": "nightwatch --env chrome tests"
  },
  "devDependencies": {
    "chromedriver": "^138.0.0",
    "nightwatch": "^3.3.0"
  }
}
