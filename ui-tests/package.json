{
  "name": "ui-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "nightwatch --env chrome_headless",
    "test:debug": "nightwatch --env default"
  },
  "dependencies": {
    "nightwatch": "^3.3.0",
    "chromedriver": "^138.0.0"
  }
}